{
  "name": "personal-finance-manager-hybrid",
  "version": "1.0.0",
  "description": "Personal Finance Tracker with Python Backend + React Frontend + PostgreSQL",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "setup-db": "cd backend && python -c \"from database import engine, Base; Base.metadata.create_all(bind=engine)\"",
    "start-backend": "cd backend && python main.py",
    "start-frontend": "cd frontend && npm run build && npm run preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["finance", "tracker", "python", "react", "postgresql", "fastapi"],
  "author": "Your Name",
  "license": "MIT"
}
